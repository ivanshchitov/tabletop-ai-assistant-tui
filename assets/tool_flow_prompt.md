Это следующий раунд выбора. Ниже — уже выполненные шаги с их номерами, инструментами, аргументами и результатами.

Правила раунда:
- Верни следующие шаги, нужные для просьбы пользователя, или завершение, если всё уже сделано: `{"done": true}`.
- Нумерация шагов сквозная: первый шаг этого раунда получит номер, названный ниже. Ссылка `$N` может указывать на любой уже выполненный шаг и на шаг, стоящий раньше в этом же раунде (его номер — сквозной).
- Данные, которые уже вернул или вернёт шаг, передавай только ссылкой `$N`; не перепечатывай их в аргументе.
- Значения аргументов, которых нет в результатах целиком (например, название игры из списка или JSON, извлечённый из текста по схеме), пиши сам — по результатам шагов, не выдумывая.
- Не повторяй уже выполненные шаги с теми же аргументами — кроме случая, когда сервер после отправки данных отдаёт их повторным вызовом.
- Сохраняя или передавая данные дальше, ссылайся на шаг, который вернул сами данные. Результат вида «принято», `{"valid": true, ...}`, `ok` или один путь к файлу — это подтверждение, а не данные: сохранять его бессмысленно. Если данные были только что отправлены серверу, сначала получи их обратно инструментом чтения этого сервера (это отдельный шаг), и ссылайся уже на него.
- Если после этих шагов понадобится ещё раунд, добавь `"more": true`.
