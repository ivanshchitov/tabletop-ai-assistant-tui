## 1. Ядро: конструкторы промптов стратегий (TDD)

- [x] 1.1 Падающий тест `tests/unit/test_strategies.py`: фиксированная задача — константа текста задачи существует, содержит волка/козу/капусту и требование минимального числа рейсов; написать минимальный модуль `core/strategies.py` с константой. Проверка: `pytest tests/unit/test_strategies.py -q`
- [x] 1.2 Падающий тест: промпт стратегии 1 (прямой) — системное сообщение нейтрально, не содержит форматных инструкций `AnswerSettings` и системного промпта вопрос-ответного режима; user-сообщение содержит дословный текст задачи. Минимальная реализация. Проверка: `pytest tests/unit/test_strategies.py -q`
- [x] 1.3 Падающий тест: промпт стратегии 2 (пошагово) — user-сообщение содержит текст задачи и инструкцию решать пошагово, без ролевых/экспертных добавок. Минимальная реализация. Проверка: `pytest tests/unit/test_strategies.py -q`
- [x] 1.4 Падающий тест: стратегия 3 (промпт от модели) — builder первого запроса (текст задачи + просьба составить промпт) и builder второго запроса (полученный промпт как system, задача как user). Минимальная реализация. Проверка: `pytest tests/unit/test_strategies.py -q`
- [x] 1.5 Падающий тест: стратегия 4 (эксперты) — три фиксированные роли (когнитивный психолог, шахматный стратег, специалист по теории игр) и builder запроса на каждого эксперта (роль как system + текст задачи как user). Минимальная реализация. Проверка: `pytest tests/unit/test_strategies.py -q`
- [x] 1.6 Падающий тест: заголовки четырёх стратегий (номер + название). Минимальная реализация. Проверка: `pytest tests/unit/test_strategies.py -q`

## 2. Команда `/logictask` в TUI (TDD)

- [x] 2.1 Падающий тест `tests/unit/test_tui_app.py`: ввод `/logictask` запускает прогон на fake-клиенте — сделано 7 запросов в правильном порядке, под каждым заголовком выведен ответ, ответ модели стратегии 3 использован во втором её запросе, каждая роль эксперта — в своём запросе. Минимальная реализация runner'а. Проверка: `pytest tests/unit/test_tui_app.py -q`
- [x] 2.2 Падающий тест: `/logictask` не пишет в историю и не увеличивает счётчик диалогов; `/logictask ещё` запускает прогон (аргумент игнорируется). Минимальная реализация. Проверка: `pytest tests/unit/test_tui_app.py -q`
- [x] 2.3 Падающий тест: ошибка API во время стратегии 3 — сообщение об ошибке, стратегия 4 не выполняется, следующий вопрос обрабатывается как обычно. Минимальная реализация. Проверка: `pytest tests/unit/test_tui_app.py -q`
- [x] 2.4 `/logictask` добавлен в `COMMANDS` (автодополнение по Tab и список в статус-баре). Проверка: `pytest tests/unit/test_tui_app.py tests/unit/test_settings_screen.py -q`

## 3. E2E-слой против stub API

- [x] 3.1 Падающий e2e-тест `tests/e2e/test_strategies.py`: реальное приложение в pty, `/logictask` против StubAPI — 7 записанных запросов, состав их промптов соответствует спеке, заголовки всех стратегий видны в скроллбеке, файл истории не изменился. Минимальная реализация при необходимости. Проверка: `pytest tests/e2e/test_strategies.py -q`
- [x] 3.2 Обновить снимки экрана (`pytest --snapshot-update`) — новая команда меняет вывод стартового экрана/статус-бара. Проверка: `pytest tests/e2e/test_screen_snapshots.py -q`

## 4. Финальная верификация

- [x] 4.1 Полный прогон `pytest` (без network-маркера) — все слои зелёные
- [x] 4.2 `openspec validate --strict` по change — артефакты валидны

## 5. Выбор стратегии перед прогоном (BREAKING, TDD)

- [x] 5.1 Падающий тест `tests/unit/test_logictask_screen.py`: редьюсер панели — `initial_state` показывает 4 стратегии, ↑/↓ двигают курсор, Enter возвращает выбранную, Esc сигнализирует отмену. Минимальный `ui/logictask_screen.py`. Проверка: `pytest tests/unit/test_logictask_screen.py -q`
- [x] 5.2 Падающие тесты `tests/unit/test_tui_app.py`: `/logictask` открывает панель (до Enter/Esc запросов нет); Enter запускает выбранную стратегию (1 запрос для стратегий 1–2, 2 для стратегии 3, 3 для стратегии 4); Esc — 0 запросов и возврат приглашения. Минимальная реализация в `ui/tui_app.py`. Проверка: `pytest tests/unit/test_tui_app.py -q`
- [x] 5.3 Переписать существующие юнит-тесты под новое поведение: старые сценарии «7 запросов за вызов» заменяются на прогон выбранной стратегии (проверки состава промптов каждого шага сохраняются). Проверка: `pytest tests/unit/test_tui_app.py -q`
- [x] 5.4 Падающие e2e-тесты `tests/e2e/test_strategies.py`: панель видна на экране; выбор стрелками + Enter → запросы только выбранной стратегии в stub; Esc → 0 запросов; история пуста. Переписать старый сценарий «7 запросов». Проверка: `pytest tests/e2e/test_strategies.py -q`
- [x] 5.5 Обновить снимки экрана (`pytest --snapshot-update`). Проверка: `pytest tests/e2e/test_screen_snapshots.py -q`
- [x] 5.6 Финальная верификация: полный `pytest` (без network) зелёный, `openspec validate --strict` по change. Проверка: `pytest -q && openspec validate add-prompt-strategies-command --strict`

## 6. Обновление промптов и описания задачи в панели (TDD)

- [x] 6.1 Падающие тесты `tests/unit/test_logictask.py`: промпт стратегии 1 содержит условие «только ответ — без решения, без шагов, без выводов»; в фиксированных экспертах шеф-повар вместо когнитивного психолога. Минимальная правка `core/logictask.py`. Проверка: `pytest tests/unit/test_logictask.py -q`
- [x] 6.2 Падающие тесты `tests/unit/test_tui_app.py` + `tests/e2e/test_strategies.py`: панель выбора описывает задачу (текст фиксированной задачи присутствует на экране/в скроллбеке). Минимальная реализация вывода описания в `ui/tui_app.py`. Проверка: `pytest tests/unit/test_tui_app.py tests/e2e/test_strategies.py -q`
- [x] 6.3 Финальная верификация: полный `pytest` (без network) зелёный, `openspec validate --strict`. Проверка: `pytest -q && openspec validate add-prompt-strategies-command --strict`

## 7. Психолог животных и независимость от настроек (TDD)

- [x] 7.1 Падающий тест `tests/unit/test_logictask.py`: шахматный стратег заменён психологом животных (взаимоотношения животных между собой и с людьми). Минимальная правка `core/logictask.py`. Проверка: `pytest tests/unit/test_logictask.py -q`
- [x] 7.2 Падающий тест `tests/unit/test_tui_app.py`: при JSON-формате и объёме 80 слов запросы `/logictask` не содержат форматных инструкций, а потолок токенов равен значению по умолчанию. Минимальная правка `ui/tui_app.py`. Проверка: `pytest tests/unit/test_tui_app.py -q`
- [x] 7.3 Финальная верификация: полный `pytest` (без network) зелёный, `openspec validate --strict`. Проверка: `pytest -q && openspec validate add-prompt-strategies-command --strict`
