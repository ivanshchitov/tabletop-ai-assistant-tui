## 1. Потолок объёма ответа

- [x] 1.1 Тест: `config.MAX_MAX_WORDS == 1000` и инвариант `MIN < DEFAULT < MAX` держится (`tests/unit/test_config.py`) — красный, затем правка `core/config.py`, зелёный
- [x] 1.2 Тест: `AnswerSettings.with_max_words(1000)` принимается без ошибки, `1001` — отклоняется с сообщением про диапазон `10..1000` (`tests/unit/test_answer_settings.py`) — прогон (граница уже параметризована через `config.MAX_MAX_WORDS`, должен стать зелёным без правок кроме 1.1); при необходимости точечная правка

## 2. Экран /settings и сообщения об ошибках

- [x] 2.1 Тест: панель `/settings` показывает подпись диапазона `10..1000` (`tests/unit/test_settings_screen.py`, `tests/unit/test_tui_app.py`) — прогон после 1.1, должен стать зелёным без правок (текст строится из констант); при необходимости точечная правка
- [x] 2.2 Тест: e2e — выход с объёмом 5000 показывает ошибку с диапазоном `10..1000`, не `10..500` (`tests/e2e/test_settings_flow.py`) — красный до правки константы, зелёный после 1.1

## 3. Документация

- [x] 3.1 Обновить `README.md` и `CLAUDE.md`: диапазон объёма `10..500` → `10..1000` везде, где упомянут
- [x] 3.2 Полный `pytest` проекта зелёный

## 4. Финал

- [x] 4.1 `/opsx:archive` — синк delta-спек (`answer-settings`, `configuration`, `settings-screen`) в основные спеки, архивация
