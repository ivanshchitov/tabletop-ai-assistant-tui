## 1. Переход на Python 3.10+

- [x] 1.1 Добавить `mcp` в `requirements.txt`; проверить установкой в окружение на Python 3.12 и импортом пакета
- [x] 1.2 Перевести матрицу CI на Python 3.11, 3.12 и 3.13 (`.github/workflows/tests.yml`); проверить, что версия 3.9 из файла исчезла
- [x] 1.3 Прогнать полную батарею на Python 3.12 и убедиться, что она зелёная до правок кода приложения

## 2. Описание серверов и переменные окружения

- [x] 2.1 Юнит-тест на реестр: `MCP_SERVERS` непуст, `DEFAULT_MCP_SERVER` входит в реестр, каждая запись содержит имя, транспорт, команду, аргументы, имена переменных с секретами и описание
- [x] 2.2 Ввести `MCPServerSpec` и реестр `MCP_SERVERS` с записью выбранного сервера и `DEFAULT_MCP_SERVER` в `core/config.py`; тест из 2.1 зелёный
- [x] 2.3 Юнит-тест на переопределение: заданные `TABLETOP_MCP_COMMAND`/`TABLETOP_MCP_ARGS` перекрывают запись реестра целиком, их отсутствие оставляет описание по умолчанию
- [x] 2.4 Реализовать разбор переменных окружения в описание сервера; тест из 2.3 зелёный

## 3. Клиент соединения

- [x] 3.1 Добавить фейковый MCP-сервер для тестов (отдельный скрипт, запускается интерпретатором прогона, отвечает по stdio, объявляет свои инструменты, умеет режим «не по протоколу»); проверить запуск вручную
- [x] 3.2 Падающий юнит-тест: клиент поднимает фейковый сервер, рукопожатие проходит, возвращаются имя сервера, его версия и версия протокола
- [x] 3.3 Реализовать `core/mcp_client.py` поверх пакета `mcp` (транспорт stdio, синхронная обёртка над асинхронным SDK, проброс переменных с секретами); тест из 3.2 зелёный
- [x] 3.4 Юнит-тест на список инструментов: возвращаются все объявленные фейковым сервером инструменты с именами и описаниями; отдельный случай — сервер без инструментов даёт пустой список и не ошибку
- [x] 3.5 Юнит-тесты на отказы: команда запуска не найдена и процесс отвечает не по протоколу — оба случая дают ошибку клиента с понятным текстом, процесс не остаётся запущенным

## 4. API агента

- [x] 4.1 Падающий юнит-тест: `mcp_report()` после успешного подключения к фейковому серверу отдаёт имя описания, команду запуска, имя и версию сервера, версию протокола и инструменты, поле ошибки пусто
- [x] 4.2 Реализовать снимок `MCPReport` и метод `mcp_report()` в `core/tabletop_agent.py`; тест из 4.1 зелёный
- [x] 4.3 Юнит-тест: при недоступном сервере снимок содержит текст ошибки и пустой список инструментов, исключение наружу не выходит
- [x] 4.4 Юнит-тест: подключение не делает ни одного запроса к модели и не меняет счётчики токенов сессии (фейковый клиент модели не получает вызовов)
- [x] 4.5 Добавить фазу запроса для индикации соединения и подпись к ней; юнит-тест на то, что фаза сообщается слушателю

## 5. Команда /mcp

- [x] 5.1 Падающий юнит-тест: `/mcp` печатает отчёт по снимку — имя и версия сервера, версия протокола, команда запуска, по строке на инструмент
- [x] 5.2 Реализовать команду в `ui/tui_app.py` и запись в `ui/commands_screen.COMMAND_OPTIONS`; тесты из 5.1 зелёные
- [x] 5.3 Юнит-тест: `/mcp` с аргументом печатает тот же отчёт; `/mcp` есть в автодополнении и в панели `/commands`
- [x] 5.4 Юнит-тест: при ошибке подключения печатается причина, приложение принимает следующий ввод
- [x] 5.5 Экранировать текст, пришедший от сервера (имя, описания инструментов, текст ошибки); юнит-тест с разметкоподобной строкой в имени инструмента

## 6. Сквозные проверки

- [x] 6.1 Пробросить `TABLETOP_MCP_COMMAND` и `TABLETOP_MCP_ARGS` в `tests/e2e/harness.AppSession`; проверить, что e2e-прогон не запускает внешний сервер
- [x] 6.2 E2e-сценарий `tests/e2e/test_mcp_flow.py`: `/commands` показывает `/mcp`; `/mcp` печатает отчёт с инструментами фейкового сервера; `/usage` подтверждает отсутствие запросов к модели; недоступный сервер даёт строку с причиной и возврат приглашения
- [x] 6.3 Контрактный тест под маркером `network`: каждая запись реестра поднимается и отдаёт непустой список инструментов; по умолчанию не запускается
- [x] 6.4 Проверить отсутствие имён инструментов конкретного сервера в `core/` и `ui/` поиском по коду
- [ ] 6.5 Полный прогон `pytest` зелёный
