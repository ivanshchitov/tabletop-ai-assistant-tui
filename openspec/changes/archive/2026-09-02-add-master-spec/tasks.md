## 1. Дельта-спеки возможностей

- [x] 1.1 Написать `specs/question-answering/spec.md`: ответы по теме, дословная фраза отказа, различие реакции в JSON-режиме, отсутствие клиентской фильтрации. Проверка: файл существует, каждый Requirement имеет хотя бы один Scenario.
- [x] 1.2 Написать `specs/answer-settings/spec.md`: три настройки с диапазонами, валидация без клампинга, распределение формат/объём по system/user, форматы compact и json, предупреждение о невалидном JSON, анти-переопределение. Проверка: файл существует, сценарии покрывают все требования.
- [x] 1.3 Написать `specs/api-integration/spec.md`: форма запроса, `max_tokens` как потолок, запрет `stop`, валидация ключа, retry-политика, обработка ответов и ошибок. Проверка: файл существует, сценарии покрывают все требования.
- [x] 1.4 Написать `specs/history-persistence/spec.md`: путь и переопределение, загрузка и реплей, немедленное сохранение, лимит 50, `/clear`, устойчивость. Проверка: файл существует, сценарии покрывают все требования.
- [x] 1.5 Написать `specs/terminal-ui/spec.md`: append-only журнал, приглашение без разметки, команды, пустой ввод/EOF, обрезка, индикация, статус-бар, ошибки API, Ctrl+C, bootstrap ключа, стартовый экран. Проверка: файл существует, сценарии покрывают все требования.
- [x] 1.6 Написать `specs/settings-screen/spec.md`: три поля, навигация, стрелки формата, числовые поля, применение на Esc, raw-режим на весь экран, чтение клавиш. Проверка: файл существует, сценарии покрывают все требования.
- [x] 1.7 Написать `specs/configuration/spec.md`: загрузка `.env`, контракт `.env.example`, переменные переопределения, поведенческие константы. Проверка: файл существует, сценарии покрывают все требования.
- [x] 1.8 Написать `specs/test-infrastructure/spec.md`: слои тестов, изоляция окружения, отрисованный экран, stub-API с записью, кассеты, снапшоты, CI. Проверка: файл существует, сценарии покрывают все требования.

## 2. Сопутствующие артефакты

- [x] 2.1 Написать `proposal.md`: мотивация, перечень восьми возможностей, решение по коду («код не меняется»). Проверка: файл существует, раздел Capabilities перечисляет все восемь новых возможностей.
- [x] 2.2 Написать `design.md`: контекст реверс-инжиниринга, три решения владельца (JSON-отказ, сессионность настроек, `.env.example`), риски. Проверка: файл существует, все решения из обсуждения зафиксированы.

## 3. Верификация изменения

- [x] 3.1 Прогнать `openspec validate add-master-spec --strict` и устранить замечания (структура заголовков, 4-хэш сценарии, наличие Purpose). Проверка: валидация проходит без ошибок.
- [x] 3.2 Прогнать `openspec status --change add-master-spec` и убедиться, что все артефакты завершены. Проверка: `isPlanningComplete: true`.
- [x] 3.3 Убедиться, что код проекта не изменён: `git status` не показывает правок вне `openspec/`. Проверка: `git status --porcelain` содержит только пути под `openspec/`.
