## REMOVED Requirements

### Requirement: Ограничение объёма истории
**Reason**: история хранится неограниченно; глубину собираемого запроса регулируют сжатие контекста и потолок токенов сессии агента (см. capability `agent`).

## ADDED Requirements

### Requirement: Конверт формата истории
Файл истории SHALL храниться как конверт `{"summary": <строка|null>, "summary_covers": <целое>, "dialogues": [<записи>]}`. Записи `dialogues` — прежнего вида: вопрос, ответ и опциональный блок метрик. Файл прежнего вида — голый список записей — SHALL читаться как конверт с `summary: null` и `summary_covers: 0`. Новая запись SHALL дописываться в `dialogues` без вытеснения прежних; `summary` и `summary_covers` SHALL обновляться при каждом сжатии контекста агента. `summary_covers` SHALL считать записи от начала списка: файл только растёт, счётчик стабилен. Устойчивость прежняя: отсутствующий файл или нечитаемый JSON читаются как пустая история без резюме, ошибка записи не фатальна.

#### Scenario: Файл старого формата читается
- **WHEN** файл содержит голый список записей без конверта
- **THEN** записи загружаются как обмены, резюме отсутствует, приложение работает

#### Scenario: Новый файл пишется конвертом
- **WHEN** выполнен успешный обмен или сжатие контекста
- **THEN** файл — конверт с `dialogues`, `summary` и `summary_covers`, прежние записи не вытеснены

#### Scenario: Битый файл
- **WHEN** файл содержит невалидный JSON
- **THEN** приложение стартует с пустой историей без резюме, без ошибки

## MODIFIED Requirements

### Requirement: Очистка командой /clear
Команда `/clear` SHALL очищать историю и в памяти, и в файле, включая сжатое резюме: файл становится конвертом с `summary: null`, `summary_covers: 0` и пустым `dialogues`, о чём сообщается пользователю. Последующий запуск SHALL начинаться с пустой историей без резюме.

#### Scenario: Очистка
- **WHEN** пользователь выполняет `/clear` при непустой истории
- **THEN** файл — пустой конверт, выводится подтверждение

### Requirement: Реплей истории восстанавливает контекст агента
Воспроизведение истории при старте SHALL засеивать контекст агента: сохранённое резюме SHALL занимать своё место в собираемом запросе, сообщения последних `compress_after` сохранённых обменов SHALL становиться ходами `user`/`assistant` стека (ход `user` — инструкциями активных настроек ответа, ход `assistant` — дословно). Обмены файла старше хвоста, не покрытые резюме, SHALL сворачиваться суммаризатором по частям при первом вопросе. Глубина контекста SHALL определяться сжатием и потолком токенов сессии, а не лимитом записей файла.

#### Scenario: Первый вопрос после рестарта несёт прошлый обмен
- **WHEN** приложение перезапущено с непустой `history.json` и пользователь задаёт первый вопрос
- **THEN** запрос содержит system, резюме (если есть), восстановленный хвост и новый user-ход

#### Scenario: Рестарт с пустой историей
- **WHEN** приложение запущено с пустой или отсутствующей историей
- **THEN** стек и резюме пусты, первый вопрос отправляется без ходов, как при первом запуске

#### Scenario: Очистка после рестарта
- **WHEN** после рестарта с непустой историей выполнена команда `/clear` и задан новый вопрос
- **THEN** файл, стек и резюме пусты, запрос содержит system и один user-ход
