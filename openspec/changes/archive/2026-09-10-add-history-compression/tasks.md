# Tasks: сжатие истории

## 1. Чистый модуль компрессии

- [x] 1.1 Падающий тест: `needs_compression` — порог в сообщениях, срабатывание на достижении
- [x] 1.2 Падающий тест: `split_for_digest` — пакет кратно парам user/assistant, последний обмен не сворачивается, нечётный хвост не сворачивается
- [x] 1.3 Падающий тест: `context_messages` — system, затем резюме (только при наличии), затем ходы
- [x] 1.4 `core/context_compressor.py` — чистые функции порога, разделения и сборки; `pytest tests/unit -q` зелёный

## 2. Конфигурация

- [x] 2.1 Падающий тест: `HISTORY_LIMIT` удалена; `DEFAULT_COMPRESS_AFTER=10`, `DEFAULT_MAX_SESSION_TOKENS=20000` с env-переопределениями; `SUMMARY_MAX_WORDS=150`
- [x] 2.2 `core/config.py` — правка констант; `pytest tests/unit -q` зелёный

## 3. Настройки ответа

- [x] 3.1 Падающий тест: `compress_after` (5..50, дефолт 10) и `max_session_tokens` (5000..50000, дефолт 20000) — целые, вне диапазона → `AnswerSettingsError`, без клампинга и округления; снимок неизменяем
- [x] 3.2 `core/answer_settings.py` — два поля + `with_compress_after`, `with_max_session_tokens`

## 4. Экран настроек

- [x] 4.1 Падающий тест: шесть полей, две новые строки с целочисленным вводом, цикл навигации по шести, ошибки диапазона сохраняют прежнее
- [x] 4.2 `ui/settings_screen.py` — новые строки редьюсера

## 5. Файл истории

- [x] 5.1 Падающий тест: конверт `{"summary", "summary_covers", "dialogues"}` пишется; голый список читается как конверт без резюме; записи не вытесняются
- [x] 5.2 Падающий тест: `summary`/`summary_covers` обновляются при сжатии; `/clear` — пустой конверт; битый файл = пустая история
- [x] 5.3 `core/history_manager.py` — конверт, удаление `limit`

## 6. Агент

- [x] 6.1 Падающий тест: порог достигнут → запрос суммаризатора (старое резюме + пакет дословно) → стек свёрнут, последний обмен цел
- [x] 6.2 Падающий тест: ошибка суммаризатора → стек и резюме нетронуты, ошибка доходит до вызывающего, следующий вопрос повторяет сжатие
- [x] 6.3 Падающий тест: резюме в следующих запросах; покрытое не суммаризуется повторно
- [x] 6.4 Падающий тест: оценка выше `max_session_tokens` → досрочное сжатие; сжимать нечего → запрос уходит как есть
- [x] 6.5 Падающий тест: расход суммаризатора в накопителе сессии; `_last_result` после `ask()` — метрика ответа
- [x] 6.6 Падающий тест: рестарт — резюме + хвост последних `compress_after` обменов, 0 запросов суммаризатора; догон по частям при первом вопросе
- [x] 6.7 Падающий тест: `reset()` опустошает стек, резюме и файл
- [x] 6.8 Падающий тест: `stack_tokens_estimate` включает резюме
- [x] 6.9 Падающий тест: `last_compression` — метрики последнего суммаризатора при сжатии (сообщения/обмены), `None` без сжатия
- [x] 6.10 Падающий тест: слушатель фаз — агент сигнализирует `COMPRESSION` перед запросом суммаризатора и `REQUEST` перед вопросом; без слушателя тихо
- [x] 6.11 `core/tabletop_agent.py` — состояние, цикл сжатия, потолок, свойства, слушатель фаз; ассет `assets/summary_prompt.md`

## 7. Терминальный слой (unit)

- [x] 7.1 Падающий тест: после сжатия печатается строка «Контекст сжат: N сообщений (M обменов) → резюме»; без сжатия — нет; в `history.json` её нет
- [x] 7.2 Падающий тест: подпись индикатора по фазе — «Суммаризация...» при сжатии, «Отправка...» при запросе
- [x] 7.3 Падающий тест: счётчик диалогов за сессию отсутствует в статус-баре; итоги сессии по токенам остаются
- [x] 7.4 `ui/tui_app.py` — строка о сжатии по `last_compression`, маппинг фазы на подпись, удаление счётчика из статус-бара

## 8. E2E (stub)

- [x] 8.1 Падающий тест: при достижении порога уходит запрос суммаризатора; последующие запросы содержат резюме и хвост без самых старых ходов
- [x] 8.2 Падающий тест: рестарт несёт сохранённое резюме; `/clear` опустошает резюме и файл
- [x] 8.3 Падающий тест: экран `/settings` показывает шесть полей
- [x] 8.4 Падающий тест: строка «Контекст сжат: …» видна в журнале при срабатывании; в реплее после рестарта её нет
- [x] 8.5 Падающий тест: индикатор показывает «Суммаризация...» во время сжатия и «Отправка...» во время запроса
- [x] 8.6 Падающий тест: счётчика «Диалогов за сессию» в статус-баре нет
- [x] 8.7 Существующие e2e-ожидания, ждущие маркер «Диалогов за сессию», переезжают на ответы и итоги сессии по токенам
- [x] 8.8 Снимки экранов переписаны сознательно: `pytest --snapshot-update` (новые строки настроек, отчёт `/usage`, без счётчика)

## 9. Сравнение (до/после) и live

- [x] 9.1 Network-тест, свободные утверждения: ранние обмены до пересечения порога (рост токенов, ответы на полном контексте) против обменов после срабатывания (плато) + вопрос по давнему обмену через резюме
- [ ] 9.2 Числа сравнения — в README дня

## 10. Финал

- [x] 10.1 Полный `pytest` зелёный
- [x] 10.2 `README.md` и `CLAUDE.md` — секции про `HISTORY_LIMIT` и потолок контекста переписаны под новую модель
- [ ] 10.3 `/opsx:archive` — только после подтверждения пользователя
