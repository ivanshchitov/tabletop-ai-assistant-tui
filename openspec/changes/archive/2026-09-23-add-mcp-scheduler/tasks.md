## 1. Хранилище планировщика

- [x] 1.1 Добавить в `core/config.py` путь `SCHEDULE_FILE` с переменной `TABLETOP_SCHEDULE_FILE` и константы
      планировщика (диапазон периода, диапазон задержки, длина журнала прогонов); проверка — тест в
      `tests/unit/test_config.py` на переопределение пути и на значения по умолчанию
- [x] 1.2 Написать `core/schedule_store.py`: конверт `{"jobs", "runs", "collected"}`, чтение отсутствующего,
      пустого и повреждённого файла как пустого планировщика, запись целиком, ограничение длины журнала
      прогонов при сохранении накопленных данных целиком; проверка — новый `tests/unit/test_schedule_store.py`
- [x] 1.3 Добавить `schedule.json` в `.gitignore` и проброс `TABLETOP_SCHEDULE_FILE` в
      `tests/e2e/harness.AppSession`; проверка — e2e-тест, что реальный `schedule.json` репозитория не
      появляется и не меняется после прогона

## 2. Инструменты сервера

- [x] 2.1 Добавить в `mcp_server/dnd_tools.py` инструмент сбора данных по разделу с накоплением: срез раздела,
      различение записей «впервые» и «уже известно», запись накопленного в хранилище; проверка — тесты в
      `tests/unit/test_dnd_tools.py` против локальной заглушки внешнего API (первый сбор, повторный сбор,
      появление новой записи)
- [x] 2.2 Написать `mcp_server/scheduler.py`: постановка задания (инструмент, аргументы, период, задержка),
      перечень заданий, выполнение просроченных с записью прогона и сдвигом срока, агрегированный отчёт;
      исполнитель вызова инжектируется; проверка — новый `tests/unit/test_scheduler.py` (просроченность,
      отложенный старт, сдвиг срока, падение одного задания, пустой планировщик)
- [x] 2.3 Валидация аргументов постановки: неизвестный инструмент, период и задержка вне диапазона,
      отсутствующий обязательный параметр — отказ текстом без создания задания; проверка — отрицательные тесты
      в `tests/unit/test_scheduler.py`
- [x] 2.4 Объявить новые инструменты в схемах сервера и подключить их в `mcp_server/dnd_server.py`; обновить
      описание записи реестра в `core/config.py`; проверка — `tests/unit/test_dnd_server.py`: сервер как
      настоящий процесс отдаёт все инструменты со схемами и выполняет вызов постановки задания
- [x] 2.5 Дополнить список имён в `tests/unit/test_mcp_client.py::test_tool_names_are_not_hardcoded_in_the_app`
      новыми инструментами; проверка — тест зелёный, имён нет в `core/` и `ui/`

## 3. Фоновый исполнитель

- [x] 3.1 Написать `tabletop-scheduler.py` в корне репозитория: режим `--once` (один прогон и выход) и цикл с
      периодом по умолчанию, вызовы через `core/mcp_client`, ошибка тика печатается и не останавливает цикл;
      проверка — e2e-тест, запускающий исполнитель с `--once` и наблюдающий появившийся прогон в файле
- [x] 3.2 Прогон исполнителя без запущенного приложения виден приложению при следующем запуске; проверка —
      e2e-тест: `--once`, затем старт приложения со строкой итога планировщика

## 4. Сводка у агента

- [x] 4.1 Передать хранилище планировщика в конструктор `TabletopAgent` (с подменой по умолчанию, как у прочих
      сторов) и добавить `schedule_report()`; проверка — тесты в `tests/unit/test_tabletop_agent.py` на снимок
      с заданиями и без них
- [x] 4.2 Добавить курсор объявленных прогонов и свойство `last_schedule`: при старте курсор на последнем
      прогоне файла, снимок обновляется по завершении хода, повторно о том же прогоне не сообщается; проверка —
      тесты в `tests/unit/test_tabletop_agent.py` (новый прогон объявлен один раз, прошлые не объявляются)

## 5. Терминал

- [x] 5.1 Добавить команду `/schedule` в `ui/commands_screen.COMMAND_OPTIONS` и печать отчёта по снимку в
      `ui/tui_app.py`, с экранированием чужого текста и без запросов к модели; проверка — юнит-тест в
      `tests/unit/test_tui_app.py` и e2e-тест на пустой и заполненный отчёт
- [x] 5.2 Печатать строку итога планировщика при запуске и строку объявления новых прогонов после ответа;
      объявление откладывается во время прогона задачи; проверка — e2e-тест: прогон исполнителя между
      вопросами даёт строку после следующего ответа и не даёт её повторно
- [x] 5.3 Строки планировщика не попадают в `history.json` и не воспроизводятся при рестарте; проверка —
      e2e-тест на содержимое файла истории после объявления

## 6. Закрытие

- [x] 6.1 Обновить снапшоты экрана, если стартовая строка меняет раскладку (`pytest --snapshot-update`) —
      осознанно, с проверкой диффа снапшота
- [x] 6.2 Полный `pytest` зелёный; `openspec validate add-mcp-scheduler --strict` зелёный
- [ ] 6.3 Обновить `CLAUDE.md` и `README.md`: блок планировщика, новая переменная окружения, новая capability,
      новые файлы в раскладке тестов; сверить существующие списки с реальностью
