## 1. Состояние задачи

- [x] 1.1 Описать `core/task_state.py`: этапы (Planning/Execution/Validation/Done), статусы задач, задача и очередь, производные «текущий шаг» и «ожидаемое действие» — тест `tests/unit/test_task_state.py` показывает поля каждого из четырёх этапов
- [x] 1.2 Реализовать переходы автомата по схеме из `design.md` (круги Planning, шаги Execution, попытки Validation, Done и следующая задача) — тест на каждую ветку схемы, включая ограничение кругов планирования и исчерпание попыток проверки
- [x] 1.3 Реализовать паузу и продолжение: признак паузы в состоянии, пауза на каждом из четырёх этапов и в ожидании правок — тест паузы на каждом этапе и продолжения с того же шага
- [x] 1.4 Реализовать хранилище `task.json`: терпимое чтение (нет файла, битый JSON, чужая форма — пустая очередь) и атомарная запись — тесты на каждый случай чтения и на запись после перехода
- [x] 1.5 Добавить `assets/task_prompt.md` и сборку системного сообщения состояния — тест: непустая очередь даёт сообщение с этапом, шагом и ожидаемым действием, пустая — `None`

## 2. Конфигурация

- [x] 2.1 Добавить `TABLETOP_TASK_FILE` и константы конвейера (предел длины цели, границы числа подзадач плана, круги планирования, попытки проверки, `TASK_MAX_WORDS`) — `tests/unit/test_config.py` проверяет путь по умолчанию, переопределение и границы
- [x] 2.2 Внести `task.json` в `.gitignore` — `git check-ignore task.json` подтверждает игнор

## 3. Промпты и разбор ответов

- [x] 3.1 Написать ассеты `assets/task_plan_prompt.md`, `assets/task_execute_prompt.md`, `assets/task_validate_prompt.md`
- [x] 3.2 Реализовать разбор ответов планирования и проверки на стороне приложения (JSON-объект в тексте, мусор, пустота, неверные типы) — юнит-тесты на каждый случай, включая «неразобранный ответ — сбой операции»

## 4. Конвейер

- [x] 4.1 Реализовать шаг Planning: запрос плана, сохранение плана, ожидание правок — юнит-тест с фальшивым запросом проверяет план из ответа и переход в ожидание правок
- [x] 4.2 Реализовать круг правок: непустой ответ строит новый план, пустой переводит в Execution, лимит кругов принимает план как есть — три теста на ветки
- [x] 4.3 Реализовать шаги Execution: одна подзадача за вызов, раздел артефакта, отметка невыполненной подзадачи с причиной — тесты на успех, сбой подзадачи и продолжение остальных
- [x] 4.4 Реализовать детерминированные проверки артефакта (нет раздела, пустой раздел, отметка невыполненной подзадачи, обрезанный ответ) — тест на каждую проверку
- [x] 4.5 Реализовать ревью проверки: вердикт и замечания из ответа, недоступное ревью не отменяет детерминированных проверок — два теста
- [x] 4.6 Реализовать цикл Validation → Execution → Validation и исчерпание попыток → Done с незакрытыми замечаниями — тесты обеих ветвей
- [x] 4.7 Реализовать Done: summary задачи и переход к следующей задаче очереди либо остановка прогона — тесты обеих ветвей
- [x] 4.8 Реализовать сбой запроса планирования: задача `не удалось` с причиной, конвейер идёт к следующей — тест проверяет и статус, и продолжение очереди

## 5. Агент

- [x] 5.1 Отдать агенту владение состоянием и конвейером: `task_step()`, запись состояния до следующего запроса — юнит-тест с фальшивым клиентом проверяет один шаг за вызов и обновлённый файл состояния
- [x] 5.2 Провести расход запросов конвейера в накопитель сессии и сообщать фазы (планирование, выполнение, проверка) — юнит-тест на учёт токенов и на полученные фазы
- [x] 5.3 Реализовать снимок `task_report()` (очередь, этап, шаг, ожидаемое действие, план с отметками, артефакт, замечания, пауза, путь файла) — юнит-тест на непустую и пустую очередь
- [x] 5.4 Добавить системное сообщение состояния задачи в стек диалогового запроса выше памяти стратегии — юнит-тест: непустая очередь даёт сообщение, пустая не меняет состав запроса
- [x] 5.5 Убедиться, что `/clear` очередь не трогает, а сбой конвейера не поднимается исключением — два юнит-теста

## 6. Терминал: команда, отчёт, статус-бар

- [x] 6.1 Добавить `/task` в список команд панели — `tests/unit/test_commands_screen.py` и снапшот панели `/commands` показывают новый пункт
- [x] 6.2 Реализовать `/task` со значимыми аргументами (`add`, `run`, `stop`, без аргументов — отчёт) — юнит-тесты `tests/unit/test_tui_app.py` на каждую подкоманду и на неизвестную подкоманду
- [x] 6.3 Реализовать отчёт `/task` из снимка агента (этапы с отметкой, очередь со статусами, план, артефакт, замечания, путь) — юнит-тест на пустую очередь и на незавершённую задачу
- [x] 6.4 Добавить строку задачи в статус-бар (номер в очереди и этап со шагом) с отсутствием строки при пустой очереди — юнит-тесты обоих случаев

## 7. Прогон, панель и пауза

- [x] 7.1 Реализовать `keyboard.read_key_nowait()` — юнит-тест в реальном псевдотерминале: без ввода возвращает пустую строку, с вводом — саму клавишу
- [x] 7.2 Реализовать рендер живой панели задачи (задача N из M, этапы с отметкой текущего, текущий шаг, ожидаемое действие, план с отметками) — юнит-тест на отрисованный текст панели
- [x] 7.3 Реализовать цикл прогона: шаги конвейера, перерисовка панели, пауза по клавише, строка паузы, summary задачи, строка окончания прогона — юнит-тест на фальшивом агенте и e2e-тест на прогон до пустой очереди
- [x] 7.4 Реализовать Ctrl+C во время прогона как паузу — e2e-тест: нажатие Ctrl+C останавливает прогон, приложение принимает ввод дальше

## 8. E2e-слой

- [x] 8.1 Добавить в `tests/e2e/harness.AppSession` путь файла задач — e2e-тест подтверждает, что реальный `task.json` репозитория не тронут
- [x] 8.2 Написать `tests/e2e/test_task_flow.py`: полный прогон двух задач без паузы — stub-сервер фиксирует порядок и состав запросов (план → выполнение подзадач → проверка, повторный план после правок) и переход ко второй задаче
- [x] 8.3 Проверить прогон с паузой: пауза клавишей на Execution, строка паузы, состояние в отчёте на месте — e2e-тест
- [x] 8.4 Проверить продолжение после паузы с того же шага — e2e-тест: первые подзадачи не переделываются, панель показывает прежние отметки
- [x] 8.5 Проверить перезапуск приложения: второй сеанс показывает ту же задачу и шаг и продолжает работу — e2e-тест с общим файлом задач
- [x] 8.6 Проверить, что `/clear` очередь не трогает, а состояние задачи уходит в диалоговый запрос — e2e-тест на записи stub-сервера

## 9. Документы

- [x] 9.1 Обновить `README.md`: команда `/task`, подкоманды, переключатель `TABLETOP_TASK_FILE`, раздел о состоянии задачи
- [x] 9.2 Обновить `CLAUDE.md`: модули `core/task_state.py` и `core/task_pipeline.py`, этапы и переходы, пауза, изоляция файла задач в тестах — сверить существующие списки команд, модулей и переменных окружения с реальностью

## 10. Проверка

- [ ] 10.1 Полный `pytest -q` зелёный (unit и e2e), вывод — из этой сессии
- [ ] 10.2 После архивации изменения: `openspec validate --all --strict` зелёный
