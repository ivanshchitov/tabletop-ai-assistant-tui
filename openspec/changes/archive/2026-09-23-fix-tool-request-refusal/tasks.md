## 1. Промпт

- [x] 1.1 Дополнить `assets/system_prompt.md`: просьбы о собственных инструментах ассистента над
      игровыми данными тематические, отказ на них не даётся; проверка — тест состава системного
      сообщения в `tests/unit/test_prompts.py`
- [x] 1.2 Дополнить `assets/tool_result_prompt.md`: когда инструмент уже вызван, ответ строится
      на его результате, а не на отказе; проверка — тот же файл тестов

## 2. Проверка на живой модели

- [x] 2.1 Добавить в `tests/e2e/test_live_api.py` (маркер `network`) проверку: просьба поставить
      задание сбора игровых данных не получает фразу отказа
- [x] 2.2 Прогнать новый тест против настоящего API

## 3. Закрытие

- [x] 3.1 Полный `pytest` зелёный; `openspec validate fix-tool-request-refusal --strict` зелёный
