# Tasks: отрицательное упоминание запрещённого слова

## 1. Проверка

- [x] 1.1 Падающий тест `tests/unit/test_invariants.py`: «все игры — без приложений» — нарушений нет; «без
      приложений … скачайте приложение» — нарушение инварианта 1; «не рекомендую Монополию» — нет; «Возьмите
      Монополию» — есть. Проверка: `pytest tests/unit/test_invariants.py -q`. Коммит.
- [x] 1.2 `core/invariants.py` — окно отрицания в `check_answer`; `CLAUDE.md`/`README.md` — фраза о проверке.
      Проверка: `pytest tests/unit -q` зелёный. Коммит.
