# Tasks: явная модель памяти агента

## 0. Правка главных спек

- [x] 0.1 `openspec/specs/agent/spec.md` — Purpose правится напрямую (дельта существующей capability Purpose не
      несёт): описать агента как владельца трёх слоёв памяти (краткосрочная, рабочая, долговременная), а не
      «памяти диалога». Проверка: `openspec validate --all --strict` валидно, в Purpose перечислены три слоя.
      Коммит.

## 1. Правила маршрутизации и слои (чистая логика)

- [x] 1.1 Падающий тест `tests/unit/test_memory_layers.py`: таблица правил — оборот об опыте даёт
      долговременную память с категорией «профиль», оборот о решении — «решения», вопрос по правилам игры без
      оборотов не даёт записей; правило опознаёт оборот в середине реплики и регистронезависимо. Проверка:
      `pytest tests/unit/test_memory_layers.py -q`. Коммит.
- [x] 1.2 Падающий тест: значение записи — предложение сработавшего оборота, обрезанное до
      `config.MEMORY_VALUE_MAX_CHARS`; повторное срабатывание того же правила заменяет значение, а не добавляет
      запись. Проверка: тот же прогон. Коммит.
- [x] 1.3 Падающий тест: `layer_messages` — пустые слои не дают сообщения; при непустых долговременная память
      идёт перед рабочей, категории помечены, рабочая — отдельной строкой; инструкция из
      `assets/memory_prompt.md` входит в сообщение. Проверка: тот же прогон. Коммит.
- [x] 1.4 Падающий тест: снимок памяти — слои, категории, ключи, значения, хранилища и перечень правил текстом.
      Проверка: тот же прогон. Коммит.
- [x] 1.5 `core/memory_layers.py` и ассет `assets/memory_prompt.md`: слои, категории, таблица правил, разбор
      реплики, сборка сообщения памяти, снимок. Проверка: `pytest tests/unit/test_memory_layers.py -q` зелёный.
      Коммит.

## 2. Конфигурация и файл долговременной памяти

- [x] 2.1 Падающий тест `tests/unit/test_config.py`: `MEMORY_FILE` выводится из `BASE_DIR`, переопределяется
      `TABLETOP_MEMORY_FILE`, есть `MEMORY_VALUE_MAX_CHARS`. Проверка: `pytest tests/unit/test_config.py -q`. Коммит.
- [x] 2.2 `core/config.py` — путь файла памяти и предел длины записи. Проверка: тест зелёный. Коммит.
- [x] 2.3 Падающий тест `tests/unit/test_long_term_memory.py`: запись с категорией пишется и читается; повторная
      запись по ключу заменяет значение; удаление по ключу и полная очистка; отсутствующий и битый файл читаются
      как пустая память; ошибка записи не поднимается. Проверка: `pytest tests/unit/test_long_term_memory.py -q`.
      Коммит.
- [x] 2.4 `core/long_term_memory.py` — хранилище долговременной памяти на одном словаре записей (ключ, значение,
      категория), с немедленной записью. Проверка: тест зелёный. Коммит.

## 3. Рабочая память в конверте истории

- [x] 3.1 Падающий тест `tests/unit/test_history_manager.py`: конверт пишется и читается с блоком `working`,
      файл без ключа `working` читается с пустой рабочей памятью, `/clear` обнуляет рабочую память. Проверка:
      `pytest tests/unit/test_history_manager.py -q`. Коммит.
- [x] 3.2 `core/history_manager.py` — блок `working` в конверте, чтение, запись, очистка. Проверка: тест зелёный.
      Коммит.

## 4. Агент: слои, маршрут, запрос, снимок, очистка

- [x] 4.1 Падающий тест `tests/unit/test_tabletop_agent.py`: реплика с оборотом о себе уходит в долговременную
      память (запись в её файле), оборот о задаче — в рабочую (блок `working` в конверте истории), реплика без
      оборотов не меняет слои; маршрут не делает запросов к API. Проверка:
      `pytest tests/unit/test_tabletop_agent.py -q`. Коммит.
- [x] 4.2 Падающий тест: запись текущей реплики видна в запросе этого же вопроса — сообщение памяти стоит выше
      ходов диалога, долговременная память выше рабочей, новый user-ход последний; при пустых слоях сообщения
      памяти нет. Проверка: тот же прогон. Коммит.
- [x] 4.3 Падающий тест: `reset()` опустошает краткосрочную и рабочую память и файл истории, но не трогает
      долговременную память; при конструировании агента рабочая память восстанавливается из конверта, а
      долговременная — из своего файла. Проверка: тот же прогон. Коммит.
- [x] 4.4 Падающий тест: слои не урезаются потолком токенов (сокращаются ходы, память идёт целиком);
      `memory_report()` отдаёт снимок без запросов к модели. Проверка: тот же прогон. Коммит.
- [x] 4.5 Падающий тест: операции памяти агента — добавление цели, добавление сведения, удаление по ключу,
      удаление всей памяти; удаление по ключу не трогает ходы диалога. Проверка: тот же прогон. Коммит.
- [x] 4.6 `core/tabletop_agent.py` — владение слоями, маршрут перед сборкой запроса, сообщение памяти в
      `_build_messages`, `memory_report()`, операции памяти, `reset()` без долговременной памяти. Проверка:
      `pytest tests/unit/test_tabletop_agent.py -q` зелёный. Коммит.

## 5. Интерфейс: команда /memory, отчёт, журнальные строки

- [x] 5.1 Падающий тест `tests/unit/test_tui_app.py`: `/memory` печатает отчёт по трём слоям с хранилищами и
      правилами, без запросов к модели; `/memory goal …`, `/memory remember …`, `/memory forget <ключ>`,
      `/memory forget all` выполняют операции и сообщают результат; неизвестная подкоманда даёт подсказку; после
      ответа печатается строка маршрута, без сработавших правил строки нет. Проверка:
      `pytest tests/unit/test_tui_app.py -q`. Коммит.
- [x] 5.2 `ui/tui_app.py` — диспетчер `/memory`, рендер отчёта из снимка, строки журнала о маршруте и
      результатах операций; `ui/commands_screen.py` — `/memory` в списке команд с описанием; подтверждение
      `/clear` сообщает о сохранённой долговременной памяти. Проверка: тест зелёный. Коммит.
- [x] 5.3 e2e `tests/e2e/test_memory_layers.py`: вопрос с оборотами пишет в слои (в журнале строка маршрута),
      `/memory` показывает записи, `/clear` оставляет долговременную память, перезапуск приложения возвращает её и
      цель задачи; снапшот панели `/commands` обновлён осознанно (`--snapshot-update`). Проверка:
      `pytest tests/e2e -q`. Коммит.

## 6. Спеки и документация

- [x] 6.1 `openspec validate add-memory-layers --strict` — изменение валидно, дельты соответствуют реализованному
      поведению. Коммит.
- [x] 6.2 `README.md` — команда `/memory` в списке команд, раздел о трёх слоях памяти, переменная
      `TABLETOP_MEMORY_FILE`, файл `memory.json` в структуре проекта; `CLAUDE.md` — модель памяти, маршрутизация,
      слои в запросе, `/clear` и долговременная память. `.gitignore` — `memory.json`. Коммит.
- [x] 6.3 Полный `pytest -q` зелёный (unit + e2e) после всех правок. Коммит.
