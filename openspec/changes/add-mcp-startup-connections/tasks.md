## 1. Реестр и переопределение окружением

- [x] 1.1 Юнит-тест: без переменных окружения разрешение реестра отдаёт все записи `MCP_SERVERS` в порядке реестра
- [x] 1.2 Юнит-тест: с заданными `TABLETOP_MCP_COMMAND`/`TABLETOP_MCP_ARGS` разрешение отдаёт ровно одну запись — из окружения, а записи реестра не используются
- [x] 1.3 Ввести `config.mcp_servers()` и перевести на него `mcp_server_spec()` (одиночный случай — первая запись разрешения); тесты 1.1–1.2 зелёные

## 2. Подключение ко всем серверам в агенте

- [x] 2.1 Падающий юнит-тест: `connect_mcp_servers()` возвращает по снимку на каждую запись разрешённого реестра, в порядке реестра (реестр подменён двумя фейковыми серверами)
- [x] 2.2 Реализовать обход реестра в `core/tabletop_agent.py` с сохранением набора снимков; тест 2.1 зелёный
- [x] 2.3 Юнит-тест: сбой одной записи не мешает остальным — её снимок несёт ошибку, остальные несут серверы и инструменты
- [x] 2.4 Юнит-тест: `mcp_reports()` после подключения отдаёт тот же набор и не запускает процессы (счётчик подключений клиента не растёт)
- [x] 2.5 Юнит-тест: повторный `connect_mcp_servers()` заменяет набор снимков результатом нового обхода
- [x] 2.6 Юнит-тест: обход не делает ни одного запроса к модели и не меняет счётчики токенов сессии

## 3. Подключение при запуске

- [x] 3.1 Падающий юнит-тест: при старте приложение подключается к серверам до первого приглашения ввода и печатает строку итога с числом поднятых серверов и числом инструментов
- [x] 3.2 Реализовать шаг подключения со спиннером в стартовой последовательности `ui/tui_app.py`; тест 3.1 зелёный
- [x] 3.3 Юнит-тест: недоступный сервер не мешает старту — приложение доходит до приглашения ввода, а строка итога называет число недоступных

## 4. Отчёт /mcp по всем серверам

- [x] 4.1 Падающий юнит-тест: `/mcp` печатает раздел на каждый сервер — имя и версия сервера, версия протокола, команда запуска, инструменты по строке
- [x] 4.2 Переписать `_print_mcp_report` под набор снимков; тест 4.1 зелёный
- [x] 4.3 Юнит-тест: `/mcp` без аргумента не запускает процессы (печатает снимки запуска), `/mcp tools` даёт тот же отчёт
- [x] 4.4 Юнит-тест: `/mcp refresh` переподключается ко всем серверам и печатает обновлённый отчёт
- [x] 4.5 Юнит-тест: раздел недоступного сервера печатает причину отказа, следующий ввод обрабатывается

## 5. Сквозные проверки

- [x] 5.1 E2e: запуск приложения с фейковым сервером печатает строку итога до приглашения ввода
- [x] 5.2 E2e: `/mcp` печатает отчёт по снимкам запуска; `/mcp refresh` печатает отчёт заново
- [x] 5.3 E2e: `/usage` после `/mcp` подтверждает отсутствие запросов к модели
- [x] 5.4 Проверить снапшоты стартового экрана: новая строка итога — осознанная смена раскладки, перезаписать `--snapshot-update` и просмотреть дифф
- [x] 5.5 Контрактный тест реестра (`network`) остаётся зелёным на всех записях
- [x] 5.6 Полный прогон `pytest` зелёный
