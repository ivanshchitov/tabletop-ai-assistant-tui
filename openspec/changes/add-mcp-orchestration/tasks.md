## 1. Реестр без BGG

- [x] 1.1 Тест: реестр — `rulebooks`, `dnd-rules`, `rule-disputes`, записи `boardgamegeek` нет, `DEFAULT_MCP_SERVER == "dnd-rules"`; поправить ожидания в `tests/unit/test_mcp_client.py`
- [x] 1.2 Удалить запись из `core/config.py`, сменить `DEFAULT_MCP_SERVER`
- [x] 1.3 Тест и код: `TABLETOP_MCP_ARGS` с ` | ` даёт несколько записей переопределения, один набор — прежняя запись
- [ ] 1.4 Коммит

## 2. Маршрутизация по каталогу

- [ ] 2.1 Тесты `mcp_tools.route`: верный сервер, исправленный маршрут, неизвестный инструмент, неоднозначный, сервер не назван
- [ ] 2.2 Реализовать `route` и `MCPToolResult.rerouted_from`, подключить в `_run_chain_step` вместо `_server_of_tool`; тест агента: ошибка маршрута не запускает процесс
- [ ] 2.3 Коммит

## 3. Флоу из раундов

- [ ] 3.1 Тесты разбора: `more`, `done`, `{"tool": null}`, пустые шаги
- [ ] 3.2 Тесты `build_round_messages`: выполненные шаги с номерами и серверами, бюджет символов режет старые с пометкой, новейший целиком
- [ ] 3.3 Реализовать разбор, сообщения раунда, `assets/tool_flow_prompt.md`, правило «результат — данные» в `assets/tool_choice_prompt.md`, константы `TOOL_FLOW_MAX_ROUNDS`/`TOOL_FLOW_MAX_STEPS`/`TOOL_FLOW_CONTEXT_CHARS`/`TOOL_FLOW_MAX_WORDS`
- [ ] 3.4 Тесты агента с поддельным клиентом: два раунда (второй запрос выбора содержит результат шага 1), сквозные `$N`, завершение моделью, предел раундов, предел шагов, сбой выбора второго раунда, без `more` — один запрос выбора; снимок `last_tool_flow` с причиной завершения
- [ ] 3.5 Реализовать цикл раундов в `_choose_and_call_tools` и `ToolFlowReport`
- [ ] 3.6 Коммит

## 4. Журнал и `/tool flow`

- [ ] 4.1 Тесты TUI: строка шага многораундового флоу с раундом и сервером, пометка исправленного маршрута, однораундовый флоу — прежний формат; `/tool flow` — отчёт и «флоу не выполнялся», ноль запросов к модели
- [ ] 4.2 Реализовать в `ui/tui_app.py`, описание `/tool` в `ui/commands_screen.py`
- [ ] 4.3 Коммит

## 5. e2e

- [ ] 5.1 `tests/fake_mcp_server.py`: режим `--second` с другим набором инструментов
- [ ] 5.2 `tests/e2e/test_tool_flow.py`: флоу из трёх раундов на двух поддельных серверах — порядок запросов выбора и вызовов, результат шага раунда 1 во втором запросе выбора, строки журнала с раундами и серверами, `/tool flow`; `test_mcp_registry.py` — реестр без BGG
- [ ] 5.3 Коммит

## 6. Закрытие

- [ ] 6.1 Полный `pytest`
- [ ] 6.2 `openspec archive add-mcp-orchestration --yes`, `openspec validate --all --strict`, коммит
- [ ] 6.3 `README.md` и `CLAUDE.md`: реестр без BGG, флоу из раундов, маршрутизация, `/tool flow`, переменная `TABLETOP_MCP_ARGS`; сверка существующих списков; коммит
