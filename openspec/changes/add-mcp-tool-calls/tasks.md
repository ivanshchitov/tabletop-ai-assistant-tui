## 1. Собственный MCP-сервер

- [x] 1.1 Локальная HTTP-заглушка внешнего API в тестах (разделы, список раздела, запись раздела, ошибка) — проверка: юнит-тест поднимает заглушку и получает от неё ожидаемый JSON
- [x] 1.2 Модуль обращения к внешнему API в `mcp_server/`: базовый адрес из переменной окружения, ветка правил параметром, ошибки сети и неизвестной записи как текст — проверка: `pytest tests/unit/test_dnd_api.py -q` зелёный, заглушка видит запрошенные пути
- [x] 1.3 Три инструмента сервера (перечень разделов, поиск по разделу, запись по идентификатору) со схемами входных параметров и валидацией аргументов до HTTP — проверка: юнит-тесты на успешный вызов, ветку правил по умолчанию и явную, отсутствующий обязательный параметр, неизвестный раздел
- [x] 1.4 Запуск сервера по stdio (`mcp_server/dnd_server.py`), объявление инструментов протоколом — проверка: юнит-тест поднимает сервер как процесс через клиент и получает три инструмента со схемами

## 2. Клиент и реестр

- [x] 2.1 Фейковый сервер тестов отвечает на `tools/call`: успешный вызов с эхом аргументов и ошибка неизвестного инструмента, инструмент с несколькими параметрами — проверка: `pytest tests/unit/test_mcp_client.py -q` зелёный на новых тестах вызова
- [x] 2.2 `MCPClient.call_tool(имя, аргументы)` и схемы входных параметров в снимке инструмента: одноразовое соединение, ошибка данными — проверка: юнит-тесты на успех, неизвестный инструмент, недоступный сервер
- [x] 2.3 Запись собственного сервера в `MCP_SERVERS` и переменная `TABLETOP_DND_API_URL` в `core/config.py` — проверка: юнит-тест на состав реестра и на то, что имён инструментов нет в `core/` и `ui/` (существующий тест остаётся зелёным)

## 3. Агент

- [x] 3.1 Снимок `MCPToolResult` и `agent.call_mcp_tool(сервер, инструмент, аргументы)`: неизвестный сервер и неизвестный инструмент возвращаются ошибкой снимка — проверка: юнит-тесты агента на успех и обе ошибки, запросов к модели ноль
- [x] 3.2 Инструкция выбора инструмента в `assets/` и разбор ответа модели (первый JSON-объект, «инструмент не нужен», мусор, неизвестное имя) — проверка: юнит-тесты разбора на каждую ветку
- [x] 3.3 Автовыбор перед вопросом: вспомогательный запрос, вызов инструмента, фаза индикации, расход в леджер сессии, сбой не блокирует ответ — проверка: юнит-тесты агента с фейковым клиентом на успешный выбор, отказ выбора, отказ вызова и выключенный режим
- [x] 3.4 Результат инструмента отдельным системным сообщением выше диалога; без вызова состав сообщений прежний; в историю не пишется — проверка: юнит-тесты на состав сообщений запроса и на содержимое файла истории

## 4. Терминальный слой

- [x] 4.1 Команда `/tool` в перечне команд и автодополнении, подкоманды `list`/`call`/`auto` с подсказкой по формату — проверка: юнит-тесты TUI на распознавание и на подсказку при неверном формате
- [x] 4.2 Печать перечня инструментов с входными параметрами по снимкам подключения, без запуска процессов — проверка: юнит-тест TUI на вывод и на отсутствие запросов к модели
- [x] 4.3 Ручной вызов `/tool call` с индикацией ожидания, печатью результата и причины отказа, с экранированием разметки — проверка: юнит-тесты TUI на успех и на отказ
- [x] 4.4 Строки журнала об автоматическом вызове (успех, неудача, инструмент не понадобился) и переключатель `auto on|off` — проверка: юнит-тесты TUI на каждую строку и на состояние переключателя

## 5. Проверки в реальном приложении

- [ ] 5.1 e2e: `/tool list` и `/tool call` в pty против фейкового сервера, `/usage` после них показывает ноль запросов к модели — проверка: `pytest tests/e2e/test_tool_flow.py -q` зелёный
- [ ] 5.2 e2e: автоматический вызов подмешивает результат в запрос (stub-API фиксирует системное сообщение), `/tool auto off` убирает вспомогательный запрос — проверка: тот же файл, утверждения по записанным запросам
- [ ] 5.3 e2e: отказ сервера при автовызове не мешает ответу, строки журнала нет в `history.json` — проверка: тот же файл
- [ ] 5.4 Контрактный тест против живого API под маркером `network`: сервер поднимается, отдаёт инструменты, каждый вызов возвращает данные — проверка: `pytest tests/e2e/test_dnd_server_live.py -m network -q` зелёный, обычный прогон его не выбирает

## 6. Закрытие

- [ ] 6.1 Полный `pytest -q` зелёный (unit + e2e), снапшоты обновлены только при осознанной смене раскладки
- [ ] 6.2 `openspec validate add-mcp-tool-calls --strict` и `openspec validate --all --strict` зелёные, изменение заархивировано с `--yes`
- [ ] 6.3 `CLAUDE.md` и `README.md`: новая команда, новая переменная окружения, каталог `mcp_server/`, новые тестовые файлы; существующие перечни сверены с реальностью
