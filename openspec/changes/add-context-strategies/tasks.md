# Tasks: стратегии управления контекстом

## 0. Правка главной спеки

- [ ] 0.1 `openspec/specs/agent/spec.md` — Purpose правится напрямую (дельта существующей capability Purpose не
      несёт): убрать решение фиксированной логической задачи и «пересылает стек модели целиком», описать агента
      как владельца памяти диалога, собирающего запрос по активной стратегии. Проверка: `openspec validate --all --strict`
      валидно, в Purpose нет слов о логической задаче. Коммит.

## 1. Чистая логика стратегий

- [ ] 1.1 Падающий тест `tests/unit/test_context_strategies.py`: окно — ходы целыми обменами, не больше N
      сообщений, последний обмен всегда в окне; окно 5 при трёх обменах даёт последние два. Проверка:
      `pytest tests/unit/test_context_strategies.py -q`. Коммит.
- [ ] 1.2 Падающий тест: блок фактов — слияние пар (новое значение ключа заменяет прежнее), предел числа ключей
      вытесняет самые старые, рендер блока в текст стабилен, парсинг JSON-ответа модели (в том числе из блока
      кода и с лишним текстом) и отказ на мусоре. Проверка: тот же прогон. Коммит.
- [ ] 1.3 Падающий тест: ветки — чекпоинт, новая ветка копирует индексы активной до чекпоинта, обмен попадает
      только в активную ветку, переключение меняет состав ходов ветки, ветка без чекпоинта начинается пустой.
      Проверка: тот же прогон. Коммит.
- [ ] 1.4 `core/context_strategies.py`: перечисление стратегий, чистые структуры и сборка вида (окно, факты,
      ветки) без I/O. Проверка: `pytest tests/unit/test_context_strategies.py -q` зелёный. Коммит.

## 2. Конфигурация и настройки

- [ ] 2.1 Падающий тест `tests/unit/test_config.py`: константы `CONTEXT_STRATEGIES` с дефолтом `summary`,
      `FACTS_MAX_WORDS=120`, `MAX_FACTS_KEYS=20`. Проверка: `pytest tests/unit/test_config.py -q`. Коммит.
- [ ] 2.2 `core/config.py` — константы. Проверка: тест зелёный. Коммит.
- [ ] 2.3 Падающий тест `tests/unit/test_answer_settings.py`: поле `context_strategy` с дефолтом, `with_context_strategy`
      принимает четыре значения, мусор → `AnswerSettingsError` без подмены на дефолт, снимок неизменяем.
      Проверка: `pytest tests/unit/test_answer_settings.py -q`. Коммит.
- [ ] 2.4 `core/answer_settings.py` — поле и `with_context_strategy`. Проверка: тест зелёный. Коммит.

## 3. Агент: стек, стратегии, факты, ветки, снимок

- [ ] 3.1 Падающий тест `tests/unit/test_tabletop_agent.py`: при стратегии резюме свёрнутые ходы остаются в
      стеке, а переключение на окно снова делает их отправимыми; запрос вопроса несёт system + вид стратегии +
      новый user-ход. Проверка: `pytest tests/unit/test_tabletop_agent.py -q`. Коммит.
- [ ] 3.2 Падающий тест: стратегия окна — ровно один запрос на вопрос (без суммаризатора), состав запроса
      соответствует окну; превышение потолка сужает окно, последний обмен не отбрасывается. Проверка: тот же
      прогон. Коммит.
- [ ] 3.3 Падающий тест: стратегия фактов — перед вопросом уходит запрос обновления, блок фактов приходит в
      запросе вопроса отдельным системным сообщением, прежнее значение ключа заменяется, факт переживает
      следующий вопрос. Проверка: тот же прогон. Коммит.
- [ ] 3.4 Падающий тест: сбой извлекателя (ошибка и не-JSON) не поднимает исключение, вопрос уходит с прежним
      блоком, необработанное сообщение уходит в следующее обновление; расход извлекателя учтён в накопителе;
      фазовый слушатель получает `FACTS_UPDATE`. Проверка: тот же прогон. Коммит.
- [ ] 3.5 Падающий тест: стратегия веток — запрос собирается из ходов активной ветки, обмен не влияет на другую
      ветку, переключение ветки меняет состав запроса. Проверка: тот же прогон. Коммит.
- [ ] 3.6 Падающий тест: `context_report()` — стратегия, окно/потолок, число ходов в ближайшем запросе, резюме,
      факты, ветки, оценка токенов; метод не делает запросов к модели. Проверка: тот же прогон. Коммит.
- [ ] 3.7 `core/tabletop_agent.py` — стратегия в конфиге, сборка вида по стратегии, цикл фактов, ветки, снимок
      контекста, фаза `FACTS_UPDATE`, ассет `assets/facts_prompt.md`. Проверка:
      `pytest tests/unit/test_tabletop_agent.py -q` зелёный. Коммит.

## 4. Файл истории

- [ ] 4.1 Падающий тест `tests/unit/test_history_manager.py`: конверт с `facts` пишется и читается, файл без
      ключа `facts` читается с пустым блоком, `/clear` обнуляет факты. Проверка:
      `pytest tests/unit/test_history_manager.py -q`. Коммит.
- [ ] 4.2 `core/history_manager.py` — блок фактов в конверте. Проверка: тест зелёный. Коммит.
- [ ] 4.3 Убрать устаревшее упоминание потолка записей истории из докстринга `core/usage.py` (`HISTORY_LIMIT`
      удалён ещё днём 9, а текст остался; главная спека `api-integration` правится этим же изменением).
      Проверка: поиск по `core/` не находит `HISTORY_LIMIT`, `pytest tests/unit/test_usage.py -q` зелёный. Коммит.

## 5. Терминальный слой

- [ ] 5.1 Падающий тест `tests/unit/test_settings_screen.py`: семь полей, строка стратегии переключается ←/→,
      цифры на ней игнорируются, цикл навигации по семи строкам, ошибка стратегии не отменяет остальные поля.
      Проверка: `pytest tests/unit/test_settings_screen.py -q`. Коммит.
- [ ] 5.2 `ui/settings_screen.py` — седьмая строка. Проверка: тест зелёный. Коммит.
- [ ] 5.3 Падающий тест `tests/unit/test_branches_screen.py`: список веток с активной, ↑/↓, Enter — выбрать,
      `c` — чекпоинт, `n` — новая ветка, Esc — отмена; прочие клавиши игнорируются. Проверка:
      `pytest tests/unit/test_branches_screen.py -q`. Коммит.
- [ ] 5.4 `ui/branches_screen.py` — редьюсер панели. Проверка: тест зелёный. Коммит.
- [ ] 5.5 Падающие тесты `tests/unit/test_tui_app.py`: `/context` печатает снимок агента без запросов к модели;
      `/branches` открывает панель, Esc — ни одного запроса, Enter меняет активную ветку в статус-баре; стратегия
      видна в статус-баре; строка «факты не обновлены» печатается при сбое и не попадает в `history.json`.
      Проверка: `pytest tests/unit/test_tui_app.py -q`. Коммит.
- [ ] 5.6 `ui/tui_app.py` — статус-бар со стратегией, рендер снимка контекста для `/context`, панель `/branches`,
      фаза «Обновление фактов...», строки о фактах; `ui/commands_screen.py` — список команд без `/logictask`,
      с `/context` и `/branches`; `ui/keyboard.py` — докстринг. Проверка: `pytest tests/unit -q` зелёный. Коммит.

## 6. Удаление /logictask

- [ ] 6.1 Удалить `tests/unit/test_logictask.py`, `tests/unit/test_logictask_screen.py`,
      `tests/e2e/test_strategies.py`. Проверка: `pytest tests/unit -q` падает только на ссылках внутри
      `test_tui_app.py` (правятся следующей задачей). Коммит.
- [ ] 6.2 `core/tabletop_agent.py` — убрать метод прогона, внутренний вызов и метку составленного промпта;
      `ui/tui_app.py` — убрать ветку диспетчера, панель выбора, прогон стратегии и импорты; удалить
      `core/logictask.py` и `ui/logictask_screen.py`. Проверка: `pytest tests/unit -q` зелёный; ввод `/logictask`
      уходит модели как обычный вопрос. Коммит.
- [ ] 6.3 `tests/unit/test_tui_app.py` — удалить сценарии прогонов, добавить «удалённая команда уходит как
      вопрос»; `tests/e2e/test_agent_context.py` — убрать проверку контекста вокруг прогона. Проверка:
      `pytest tests/unit/test_tui_app.py tests/e2e/test_agent_context.py -q`. Коммит.

## 7. E2E (stub)

- [ ] 7.1 Падающий e2e-тест: сценарий вопросов на стратегии окна в pty — запросы содержат только последние
      сообщения, суммаризатор не вызывается. Проверка: `pytest tests/e2e -q -k window`. Коммит.
- [ ] 7.2 Падающий e2e-тест: на стратегии фактов перед вопросом уходит запрос обновления, блок фактов виден в
      запросе вопроса, `/context` показывает пары. Проверка: `pytest tests/e2e -q -k facts`. Коммит.
- [ ] 7.3 Падающий e2e-тест: панель `/branches` видна на экране, `c` и `n` создают ветку, Enter переключает, счёт
      запросов в stub соответствует ожиданиям. Проверка: `pytest tests/e2e -q -k branches`. Коммит.
- [ ] 7.4 Проверить обновившиеся экраны (статус-бар со стратегией, экран настроек) и при расхождении раскладки
      перезаписать снимки. Проверка: `pytest --snapshot-update`, затем `pytest tests/e2e/test_screen_snapshots.py -q`
      и полный `pytest tests/e2e -q` зелёные. Коммит.

## 8. Документация

- [ ] 8.1 `README.md` — четыре стратегии, `/context`, `/branches`, новая строка настроек, удаление `/logictask`,
      структура проекта без модуля задачи. Проверка: поиск по `README.md` не находит `/logictask` и модулей
      задачи. Коммит.
- [ ] 8.2 `CLAUDE.md` — стек как append-only, стратегии и их память, факты и их извлечение, ветки, изоляция
      агента через снимок, удаление прогонов из описания архитектуры и тестов. Проверка: поиск по `CLAUDE.md`
      не находит `/logictask`. Коммит.

## 9. Сравнение и финал

- [ ] 9.1 Live-прогон одного сценария 10–15 сообщений на каждой из четырёх стратегий (порог 5 сообщений),
      фиксация качества, стабильности, `prompt_tokens` и стоимости из `/usage` для таблицы сравнения дня 10.
      Проверка: таблица заполнена по каждой стратегии, числа взяты из отчётов приложения. Коммит.
- [ ] 9.2 Полный `pytest` (без `network`) зелёный; `openspec validate add-context-strategies --strict` валидно.
      Проверка: вывод обеих команд. Коммит.
