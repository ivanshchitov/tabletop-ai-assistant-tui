## MODIFIED Requirements

### Requirement: Поведенческие константы
Приложение SHALL использовать фиксированные константы: список доступных моделей (`deepseek-v4-flash`,
`deepseek-v4-pro`, `kimi-k2.5`, `glm-5.1`, `mimo-v2.5-free`, `kimi-k2.6`, `kimi-k3`) с моделью по умолчанию
`deepseek-v4-flash`, список стратегий управления контекстом (`summary`, `sliding_window`, `sticky_facts`,
`branching`) со стратегией по умолчанию `summary`, температура по умолчанию 0.7 с допустимым диапазоном настройки
0.0..2.0, не более 3 попыток запроса, максимум 2000 символов на вопрос, потолок объёма резюме — 150 слов,
потолок объёма блока фактов — 120 слов и предел числа ключей блока фактов — 20. Глубина контекста сессии
ограничивается активной стратегией управления контекстом и потолком токенов сессии, а не числом записей истории.

#### Scenario: Список моделей фиксирован
- **WHEN** приложение формирует панель `/models`
- **THEN** список состоит из семи перечисленных моделей, модель по умолчанию — `deepseek-v4-flash`

#### Scenario: Список стратегий фиксирован
- **WHEN** пользователь открывает строку стратегии на экране `/settings`
- **THEN** доступны ровно четыре стратегии, активна `summary`, пока пользователь её не сменил

#### Scenario: Предел блока фактов
- **WHEN** блок фактов достигает 20 ключей
- **THEN** новые ключи замещают самые старые по времени появления, а число ключей не превышает 20

#### Scenario: Число попыток при таймаутах
- **WHEN** все попытки запроса истекают по таймауту
- **THEN** выполнено не более 3 попыток, после чего сообщается об ошибке

#### Scenario: Границы настроек
- **WHEN** пользователь проверяет допустимые значения на экране настроек
- **THEN** объём ограничен диапазоном 10..1000, лимит списка — 1..10, температура — 0.0..2.0, порог сжатия — 5..50, потолок контекста — 5000..50000
