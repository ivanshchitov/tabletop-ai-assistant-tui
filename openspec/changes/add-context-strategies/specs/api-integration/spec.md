## MODIFIED Requirements

### Requirement: Форма запроса — массив сообщений с контекстом сессии
Запрос SHALL выполняться методом POST на эндпоинт chat/completions (по умолчанию
`https://opencode.ai/zen/v1/chat/completions`) с заголовком `Authorization: Bearer <ключ>` и JSON-телом,
содержащим: модель, выбранную в сессии через `/models` (по умолчанию `deepseek-v4-flash`), массив `messages` —
системное сообщение (собранное из активных настроек ответа), затем контекст по активной стратегии управления
контекстом (см. `context-strategies`), затем новый user-ход, температуру из активной настройки сессии (по
умолчанию 0.7) и `max_tokens`. При стратегии фактов перед ходами SHALL добавляться отдельное системное сообщение
с блоком фактов. Глубину контекста задают стратегия и потолок токенов сессии, а не число записей истории.

#### Scenario: Успешный вопрос
- **WHEN** пользователь выбрал модель через `/models` и задаёт вопрос с активными настройками
- **THEN** к API уходит POST-запрос с выбранной моделью, массивом сообщений (system, контекст активной стратегии и новый user-ход), температурой из настройки сессии (по умолчанию 0.7) и max_tokens

#### Scenario: Модель по умолчанию до выбора
- **WHEN** пользователь не открывал `/models` и задаёт вопрос
- **THEN** запрос содержит модель `deepseek-v4-flash`

#### Scenario: История сессии отправляется
- **WHEN** задан третий вопрос за сессию после двух успешных обменов
- **THEN** запрос содержит system, два предыдущих обмена (user и assistant каждый) и новый user-ход

#### Scenario: Глубина контекста ограничена
- **WHEN** в сессии больше обменов, чем помещается в границу активной стратегии, и задан новый вопрос
- **THEN** массив `messages` содержит только system и те ходы, которые попали в границу стратегии (окно сообщений или потолок токенов)

#### Scenario: Блок фактов в запросе
- **WHEN** активна стратегия фактов и блок фактов непуст
- **THEN** массив `messages` содержит системное сообщение настроек, затем отдельное системное сообщение с блоком фактов, затем ходы контекста и новый user-ход

#### Scenario: Температура из настройки
- **WHEN** пользователь установил температуру 1.2 и отправляет вопрос
- **THEN** тело запроса содержит temperature = 1.2
