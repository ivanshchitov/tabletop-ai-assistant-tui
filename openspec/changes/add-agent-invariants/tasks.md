# Tasks: инварианты агента

## 1. Модуль инвариантов: таблица, проверка, сообщение

- [x] 1.1 Падающий тест `tests/unit/test_invariants.py`: таблица — шесть инвариантов с номерами 1..6 по
      порядку, у каждого непустое правило, у четырёх есть запрещённые слова, у двух — нет; на каждый инвариант
      со словами положительный и отрицательный пример ответа (в любом регистре и падеже); проверка ответа —
      без слов нарушений нет, с словом — нарушение с номером, правилом и оборотом таблицы, два слова разных
      инвариантов — два нарушения; ответ с «Не могу предложить» в первых 200 символах (в том числе внутри
      JSON-блока) не проверяется, а с фразой дальше окна — проверяется. Проверка:
      `pytest tests/unit/test_invariants.py -q`. Коммит.
- [x] 1.2 Падающий тест: сообщение инвариантов перечисляет все правила с номерами, содержит инструкцию из
      `assets/invariants_prompt.md` (фиксированное начало отказа, приоритет над профилем и репликой) и не
      содержит запрещённых слов как списка; текст отказа приложения начинается с «Не могу предложить» и
      называет номер, правило и слово; текст повторного запроса перечисляет нарушения. Проверка: тот же
      прогон. Коммит.
- [x] 1.3 `core/invariants.py` и ассет `assets/invariants_prompt.md` — датакласс инварианта, таблица,
      `check_answer`, `is_refusal`, `invariants_message`, `refusal_text`, `retry_prompt`; `core/config.py` —
      `INVARIANT_RETRIES = 1` с тестом в `tests/unit/test_config.py`. Проверка:
      `pytest tests/unit/test_invariants.py tests/unit/test_config.py -q` зелёный. Коммит.

## 2. Агент: сообщение в запросе, проверка ответа, повтор и отклонение

- [x] 2.1 Падающий тест `tests/unit/test_tabletop_agent.py`: запрос вопроса несёт системное сообщение
      инвариантов сразу после профиля (и сразу после system при пустом профиле), выше сообщения задачи и
      памяти, ход пользователя последний; `/clear` ничего не меняет — следующий вопрос снова несёт сообщение;
      запрос конвейера (план, подзадача, проверка) несёт сообщение вторым. Обновить существующие ожидания
      состава сообщений с комментарием о смене контракта. Проверка: `pytest tests/unit/test_tabletop_agent.py
      -q`. Коммит.
- [x] 2.2 Падающий тест: фейковый клиент отвечает с запрещённым словом, затем чисто — сделано два запроса,
      второй содержит ход `assistant` с нарушившим ответом и ход `user` с перечнем нарушений, пользователь
      получает второй ответ, стек и история хранят его, `last_invariants` — нарушение первого, повтор был,
      отклонения не было; оба ответа с нарушением — два запроса, результат — отказ приложения с номером и
      словом, стек и история хранят отказ, `last_invariants.rejected`; ошибка API на повторе — `APIError`
      наверх, стек и история без изменений; отказ модели с «Не могу предложить» и словом — один запрос, без
      повтора; расход обоих запросов в накопителе; `invariants_report()` отдаёт таблицу без запросов.
      Проверка: тот же прогон. Коммит.
- [x] 2.3 `core/tabletop_agent.py` — `_invariants_message` в `_build_messages` и в `_ask_task`, проверка в
      `ask()` с повтором и отклонением, `InvariantsCheck`/`InvariantsReport`, `last_invariants`,
      `invariants_report()`. Проверка: `pytest tests/unit/test_tabletop_agent.py -q` зелёный. Коммит.

## 3. Интерфейс: команда /invariants и строки о нарушении

- [ ] 3.1 Падающий тест `tests/unit/test_tui_app.py`: `/invariants` печатает отчёт (шесть правил с номерами и
      словами, пометка «только модель» у правил без слов, место в запросе, повтор и отклонение) без запросов
      к модели, аргументы игнорируются; после ответа без нарушений строк об инвариантах нет и статус-бар
      прежний; при повторе — строка «⛔ Инвариант N нарушен («слово») — повторный запрос»; при отклонении —
      она же плюс «⛔ Ответ отклонён: инвариант N …», предупреждение о JSON при JSON-формате не печатается.
      Проверка: `pytest tests/unit/test_tui_app.py -q`. Коммит.
- [ ] 3.2 `ui/tui_app.py` — диспетчер `/invariants`, рендер отчёта из снимка, строки о нарушении после
      ответа; `ui/commands_screen.py` — `/invariants` в списке команд; ожидаемый список в
      `tests/unit/test_commands_screen.py`. Проверка: `pytest tests/unit/test_tui_app.py
      tests/unit/test_commands_screen.py -q` зелёный. Коммит.

## 4. E2E: инварианты в живом приложении

- [ ] 4.1 `tests/e2e/test_invariants.py`: запрос к stub несёт системное сообщение инвариантов после профиля
      (payload); stub отвечает с «Монополия», затем чисто — на экране строка о повторном запросе и второй
      ответ, stub видит два запроса; два ответа с нарушением — строка об отклонении и отказ приложения на
      экране, `history.json` хранит отказ; отказ stub с «Не могу предложить» — один запрос и без строк;
      `/invariants` печатает отчёт без запросов; `/clear` не меняет сообщение в следующем запросе; запрос
      конвейера `/task run` несёт сообщение инвариантов вторым. Проверка: `pytest tests/e2e/test_invariants.py
      -q`. Коммит.
- [ ] 4.2 Существующие e2e-ожидания состава сообщений (число системных сообщений, индекс user-хода) и
      снапшоты панели `/commands` обновлены осознанно с комментарием; проверка: `pytest tests/e2e -q`
      зелёный. Коммит.

## 5. Спеки, документация, закрытие

- [ ] 5.1 `openspec validate add-agent-invariants --strict` и `openspec validate --all --strict` — изменение
      и спеки валидны, дельты соответствуют реализованному поведению. Коммит.
- [ ] 5.2 `README.md` — команда `/invariants`, раздел об инвариантах (таблица, двойная защита, отказ);
      `CLAUDE.md` — блок об инвариантах (таблица в коде, окно отказа, повтор и отклонение, место в запросе,
      конвейер), список команд и test layout. Сверить существующие списки команд и capabilities с
      реальностью. Коммит.
- [ ] 5.3 Полный `pytest -q` (unit + e2e) зелёный после всех правок. Коммит.
