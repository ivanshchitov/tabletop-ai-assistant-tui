# Tasks: персонализация агента — профиль пользователя

## 1. Модуль профиля: хранилище, диалог, сообщение

- [x] 1.1 Падающий тест `tests/unit/test_user_profile.py`: хранилище профилей — профиль пишется и читается с
      разделами; несколько профилей живут в одном файле, активный записан; переключение активного и удаление
      профиля видны в файле сразу; отсутствующий и битый файл читаются как пустой набор профилей без ошибки.
      Проверка: `pytest tests/unit/test_user_profile.py -q`. Коммит.
- [x] 1.2 Падающий тест: скрипт диалога настройки — ровно пять вопросов в порядке имя, стиль, ограничения, опыт,
      жанры и механики; ответ становится содержимым своего раздела; пустой ответ оставляет раздел как был;
      пустое имя даёт свободное имя по порядку; ответ длиннее `config.PROFILE_VALUE_MAX_CHARS` обрезается;
      повторная настройка под тем же именем заменяет отвеченные разделы и оставляет прочие. Проверка: тот же
      прогон. Коммит.
- [x] 1.3 Падающий тест: сборка системного сообщения профиля — пустой профиль даёт `None`, непустой перечисляет
      разделы с подписями, инструкция из `assets/profile_prompt.md` входит в сообщение и подчиняет предпочтения
      настройкам приложения. Проверка: тот же прогон. Коммит.
- [x] 1.4 `core/user_profile.py` и ассет `assets/profile_prompt.md` — модель профиля, хранилище, скрипт диалога
      с автоматом ответов, сборка сообщения. Проверка: `pytest tests/unit/test_user_profile.py -q` зелёный. Коммит.

## 2. Конфигурация и файл состояния

- [x] 2.1 Падающий тест `tests/unit/test_config.py`: `PROFILE_FILE` выводится из `BASE_DIR`, переопределяется
      `TABLETOP_PROFILE_FILE`, задан `PROFILE_VALUE_MAX_CHARS`. Проверка: `pytest tests/unit/test_config.py -q`.
      Коммит.
- [x] 2.2 `core/config.py` — путь файла профилей и предел длины раздела; `.gitignore` — `profile.json`.
      Проверка: тест зелёный, `profile.json` не в списке отслеживаемых файлов. Коммит.

## 3. Агент: профиль в запросе, операции, снимок

- [x] 3.1 Падающий тест `tests/unit/test_tabletop_agent.py`: при непустом профиле запрос несёт системное
      сообщение профиля сразу после system настроек и выше сообщения памяти, ход пользователя последний; два
      вопроса подряд несут профиль оба; пустой профиль сообщения не даёт и форма запроса не меняется.
      Проверка: `pytest tests/unit/test_tabletop_agent.py -q`. Коммит.
- [x] 3.2 Падающий тест: операции агента — переключение активного профиля меняет разделы в следующем запросе,
      неизвестное имя не создаёт профиль, удаление профиля убирает его из файла; `profile_report()` отдаёт
      снимок без запросов к модели; `/clear` профиль и его файл не трогает, а следующий вопрос после очистки
      по-прежнему несёт профиль. Проверка: тот же прогон. Коммит.
- [x] 3.3 `core/tabletop_agent.py` — владение хранилищем профиля, сообщение профиля в `_build_messages`, методы
      переключения и удаления, `profile_report()`, `reset()` без профиля. Проверка:
      `pytest tests/unit/test_tabletop_agent.py -q` зелёный. Коммит.

## 4. Интерфейс: команда /profile, диалог настройки, отчёт

- [x] 4.1 Падающий тест `tests/unit/test_tui_app.py`: `/profile` печатает отчёт (активный профиль и разделы,
      все профили файла, путь файла, подсказка по подкомандам) без запросов к модели и без активного профиля не
      падает; `/profile setup` задаёт пять вопросов с подменённым вводом, печатает строку о разделе после
      каждого ответа, строку о неизменённом разделе на пустой ответ и строку о сохранённом профиле в конце;
      прерывание с клавиатуры отменяет настройку, профиль не пишется и приложение продолжает работу;
      `/profile use` и `/profile forget` сообщают результат, неизвестная подкоманда даёт подсказку. Проверка:
      `pytest tests/unit/test_tui_app.py -q`. Коммит.
- [x] 4.2 `ui/tui_app.py` — диспетчер `/profile`, диалог настройки поверх автомата (ввод строкой, локальная
      отмена), рендер отчёта из снимка, строки журнала; `ui/commands_screen.py` — `/profile` в списке команд;
      подтверждение `/clear` говорит о сохранённом профиле. Проверка: тест зелёный. Коммит.
- [x] 4.3 Ожидаемый список команд обновлён в `tests/unit/test_commands_screen.py` (снапшота панели
      `/commands` в `tests/e2e/snapshots/` нет — менялись только unit-ожидания), проверка:
      `pytest tests/unit/test_commands_screen.py tests/e2e/test_screen_snapshots.py -q` зелёный. Коммит.

## 5. E2E: диалог настройки в живом приложении

- [x] 4.4 Статус-бар показывает активный профиль рядом с моделью и только когда профиль непуст;
      имена и значения профиля печатаются через `rich.markup.escape` (значение вида `[/dim]` иначе
      ломало печать). Проверка: `pytest tests/unit/test_tui_app.py -q` зелёный. Коммит.

- [x] 5.1 `tests/e2e/harness.py` — `TABLETOP_PROFILE_FILE` в окружении `AppSession` и фикстура файла профилей;
      проверка — `pytest tests/e2e/test_session.py -q` зелёный, реальный `profile.json` репозитория не создаётся.
      Коммит.
- [x] 5.2 E2E `tests/e2e/test_profile.py`: `/profile setup` в псевдотерминале проходит пять вопросов и записывает
      профиль в файл прогона; следующий вопрос несёт системное сообщение профиля (stub видит payload); два
      профиля дают разные системные сообщения на один и тот же вопрос; `/clear` профиль не трогает; перезапуск
      приложения показывает тот же профиль; файл `profile.json` в корне репозитория не создан. Проверка:
      `pytest tests/e2e/test_profile.py -q`. Коммит.

## 6. Спеки, документация, закрытие

- [x] 6.1 `openspec validate add-user-profile --strict` и `openspec validate --all --strict` — изменение и спеки
      валидны, дельты соответствуют реализованному поведению. Коммит.
- [x] 6.2 `README.md` — команда `/profile`, раздел о персонализации и диалоге настройки, переменная
      `TABLETOP_PROFILE_FILE`, файл `profile.json` в структуре проекта; `CLAUDE.md` — профиль как слой,
      диалог настройки, место профиля в запросе, `/clear` и профиль, тестовый layout. Сверить существующие
      списки команд и переменных с реальностью. Коммит.
- [x] 6.3 Полный `pytest -q` (unit + e2e) зелёный после всех правок. Коммит.
